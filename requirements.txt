# Core: ML model + tournament simulation
numpy>=1.26
pandas>=2.1
scikit-learn>=1.4
joblib>=1.3
matplotlib>=3.8

# Notebook front-end
jupyterlab>=4.0
ipykernel>=6.29

# Deep-learning base learner (auto-detected; the ensemble runs fine without it).
# CPU build is plenty: `pip install torch --index-url https://download.pytorch.org/whl/cpu`
torch>=2.6

# Optional gradient boosters (auto-detected; the pipeline runs fine without them)
# xgboost>=2.0
# lightgbm>=4.1
